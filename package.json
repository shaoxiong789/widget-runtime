{
  "name": "root",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "example": "npx lerna exec --scope example -- npm run dev",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
